import 'package:flutter/material.dart';

/// Helper class for managing themes and colors.
/// Represents the ThemeHelper class, which encapsulates its respective functionality.
class ThemeHelper {
  /// Defines `Color(0xFF0D0D0A)`, a constant/variable used within the application.
  // static const primary = Color(0xFF0D0D0A);
  /// Defines `Color(0xFF141413)`, a constant/variable used within the application.
  static const primary = Color(0xFF141413);

  /// Defines `Color(0xFFFFFFFF)`, a constant/variable used within the application.
  static const secondary = Color(0xFFFFFFFF);

  /// Defines `Color(0xffF2F2F0)`, a constant/variable used within the application.
  static const thirdBackground = Color(0xffF2F2F0);

  /// Defines `Color(0xff009CA6)`, a constant/variable used within the application.
  static const primaryBlue = Color(0xff009CA6);

  static const primaryCyan = Color(0xff03B4BF);
  // The current app theme
}

/// Class containing custom colors for a primary theme.
/// Represents the PrimaryColors class, which encapsulates its respective functionality.
class PrimaryColors {
  // Amber
  /// Defines `Color(0XFFF9BF18)`, a constant/variable used within the application.
  Color get amber500 => const Color(0XFFF9BF18);

  /// Defines `Color(0XFFE9B00A)`, a constant/variable used within the application.
  Color get amber600 => const Color(0XFFE9B00A);

  // Black
  /// Defines `Color(0XFF060605)`, a constant/variable used within the application.
  Color get black900 => const Color(0XFF060605);

  /// Defines `Color(0XFF0D0D0A)`, a constant/variable used within the application.
  Color get black90001 => const Color(0XFF0D0D0A);

  /// Defines `Color(0XFF070701)`, a constant/variable used within the application.
  Color get black90002 => const Color(0XFF070701);

  /// Defines `Color(0XFF0C0C0A)`, a constant/variable used within the application.
  Color get black90003 => const Color(0XFF0C0C0A);

  /// Defines `Color(0XFF020200)`, a constant/variable used within the application.
  Color get black90004 => const Color(0XFF020200);

  /// Defines `Color(0XFF000000)`, a constant/variable used within the application.
  Color get black90005 => const Color(0XFF000000);

  // Blue
  /// Defines `Color(0XFF59B1E2)`, a constant/variable used within the application.
  Color get blue300 => const Color(0XFF59B1E2);

  // BlueGray
  /// Defines `Color(0XFF455857)`, a constant/variable used within the application.
  Color get blueGray700 => const Color(0XFF455857);

  /// Defines `Color(0XFF506262)`, a constant/variable used within the application.
  Color get blueGray70001 => const Color(0XFF506262);

  /// Defines `Color(0XFF2D5A45)`, a constant/variable used within the application.
  Color get blueGray800 => const Color(0XFF2D5A45);

  /// Defines `Color(0XFF313664)`, a constant/variable used within the application.
  Color get blueGray80001 => const Color(0XFF313664);

  /// Defines `Color(0XFF224C64)`, a constant/variable used within the application.
  Color get blueGray80002 => const Color(0XFF224C64);

  /// Defines `Color(0XFF353535)`, a constant/variable used within the application.
  Color get blueGray900 => const Color(0XFF353535);

  // Cyan
  /// Defines `Color(0XFFE6F8F9)`, a constant/variable used within the application.
  Color get cyan50 => const Color(0XFFE6F8F9);

  /// Defines `Color(0XFFE6F5F6)`, a constant/variable used within the application.
  Color get cyan5001 => const Color(0XFFE6F5F6);

  /// Defines `Color(0XFF03B3BF)`, a constant/variable used within the application.
  Color get cyan600 => const Color(0XFF03B3BF);

  /// Defines `Color(0XFF009CA6)`, a constant/variable used within the application.
  Color get cyan700 => const Color(0XFF009CA6);

  /// Defines `Color(0XFF005E64)`, a constant/variable used within the application.
  Color get cyan900 => const Color(0XFF005E64);

  // DeepOrange
  /// Defines `Color(0XFFFBBABA)`, a constant/variable used within the application.
  Color get deepOrange100 => const Color(0XFFFBBABA);

  /// Defines `Color(0XFFF1B6A1)`, a constant/variable used within the application.
  Color get deepOrange200 => const Color(0XFFF1B6A1);

  /// Defines `Color(0XFFFBEDE7)`, a constant/variable used within the application.
  Color get deepOrange50 => const Color(0XFFFBEDE7);

  /// Defines `Color(0XFFF15A24)`, a constant/variable used within the application.
  Color get deepOrange600 => const Color(0XFFF15A24);

  /// Defines `Color(0XFFDA4713)`, a constant/variable used within the application.
  Color get deepOrange800 => const Color(0XFFDA4713);

  /// Defines `Color(0XFFC1481D)`, a constant/variable used within the application.
  Color get deepOrange900 => const Color(0XFFC1481D);

  /// Defines `Color(0XFFAF3A0F)`, a constant/variable used within the application.
  Color get deepOrange90001 => const Color(0XFFAF3A0F);

  // Gray
  /// Defines `Color(0XFFF4F4F4)`, a constant/variable used within the application.
  Color get gray100 => const Color(0XFFF4F4F4);

  /// Defines `Color(0XFFF1F1EF)`, a constant/variable used within the application.
  Color get gray200 => const Color(0XFFF1F1EF);

  /// Defines `Color(0XFFF2F2F0)`, a constant/variable used within the application.
  Color get gray20001 => const Color(0XFFF2F2F0);

  /// Defines `Color(0XFFE3E3D8)`, a constant/variable used within the application.
  Color get gray300 => const Color(0XFFE3E3D8);

  /// Defines `Color(0XFFE7E7DB)`, a constant/variable used within the application.
  Color get gray30001 => const Color(0XFFE7E7DB);

  /// Defines `Color(0XFFDDDDD7)`, a constant/variable used within the application.
  Color get gray30002 => const Color(0XFFDDDDD7);

  /// Defines `Color(0XFFB3B3A5)`, a constant/variable used within the application.
  Color get gray400 => const Color(0XFFB3B3A5);

  /// Defines `Color(0XFFBEBEB4)`, a constant/variable used within the application.
  Color get gray40001 => const Color(0XFFBEBEB4);

  /// Defines `Color(0XFF999999)`, a constant/variable used within the application.
  Color get gray500 => const Color(0XFF999999);

  /// Defines `Color(0XFF9E9E96)`, a constant/variable used within the application.
  Color get gray50001 => const Color(0XFF9E9E96);

  /// Defines `Color(0XFF74746E)`, a constant/variable used within the application.
  Color get gray600 => const Color(0XFF74746E);

  /// Defines `Color(0XFF7C7B72)`, a constant/variable used within the application.
  Color get gray60001 => const Color(0XFF7C7B72);

  /// Defines `Color(0XFF61615A)`, a constant/variable used within the application.
  Color get gray700 => const Color(0XFF61615A);

  /// Defines `Color(0XFF60605A)`, a constant/variable used within the application.
  Color get gray70001 => const Color(0XFF60605A);

  /// Defines `Color(0XFF66411E)`, a constant/variable used within the application.
  Color get gray800 => const Color(0XFF66411E);

  /// Defines `Color(0X00454444)`, a constant/variable used within the application.
  Color get gray80000 => const Color(0X00454444);

  /// Defines `Color(0XFF554C2D)`, a constant/variable used within the application.
  Color get gray80001 => const Color(0XFF554C2D);

  /// Defines `Color(0XFF5C312B)`, a constant/variable used within the application.
  Color get gray80002 => const Color(0XFF5C312B);

  /// Defines `Color(0XFF3B503C)`, a constant/variable used within the application.
  Color get gray80003 => const Color(0XFF3B503C);

  /// Defines `Color(0XFF5F212C)`, a constant/variable used within the application.
  Color get gray80004 => const Color(0XFF5F212C);

  /// Defines `Color(0XFF6A2414)`, a constant/variable used within the application.
  Color get gray80005 => const Color(0XFF6A2414);

  /// Defines `Color(0XFF50583A)`, a constant/variable used within the application.
  Color get gray80006 => const Color(0XFF50583A);

  /// Defines `Color(0XFF4A4A46)`, a constant/variable used within the application.
  Color get gray80007 => const Color(0XFF4A4A46);

  /// Defines `Color(0XFF623B16)`, a constant/variable used within the application.
  Color get gray80008 => const Color(0XFF623B16);

  /// Defines `Color(0XFF573030)`, a constant/variable used within the application.
  Color get gray80009 => const Color(0XFF573030);

  /// Defines `Color(0XFF141413)`, a constant/variable used within the application.
  Color get gray900 => const Color(0XFF141413);

  /// Defines `Color(0XFF3D1506)`, a constant/variable used within the application.
  Color get gray90001 => const Color(0XFF3D1506);

  /// Defines `Color(0XFF2C2B04)`, a constant/variable used within the application.
  Color get gray90002 => const Color(0XFF2C2B04);

  /// Defines `Color(0XFF1C1C03)`, a constant/variable used within the application.
  Color get gray90003 => const Color(0XFF1C1C03);

  /// Defines `Color(0XFF232322)`, a constant/variable used within the application.
  Color get gray90004 => const Color(0XFF232322);

  /// Defines `Color(0XFF002426)`, a constant/variable used within the application.
  Color get gray90005 => const Color(0XFF002426);

  /// Defines `Color(0XFF2E2E05)`, a constant/variable used within the application.
  Color get gray90006 => const Color(0XFF2E2E05);

  // Green
  /// Defines `Color(0XFF66BC93)`, a constant/variable used within the application.
  Color get green300 => const Color(0XFF66BC93);

  /// Defines `Color(0XFF71E2AC)`, a constant/variable used within the application.
  Color get greenA200 => const Color(0XFF71E2AC);

  /// Defines `Color(0XFF6BE9AD)`, a constant/variable used within the application.
  Color get greenA20001 => const Color(0XFF6BE9AD);

  /// Defines `Color(0XFF08DA75)`, a constant/variable used within the application.
  Color get greenA400 => const Color(0XFF08DA75);

  // Indigo
  /// Defines `Color(0XFF8B96FF)`, a constant/variable used within the application.
  Color get indigoA100 => const Color(0XFF8B96FF);

  /// Defines `Color(0XFF7B88FA)`, a constant/variable used within the application.
  Color get indigoA10001 => const Color(0XFF7B88FA);

  // LightBlue
  /// Defines `Color(0XFF53BDF8)`, a constant/variable used within the application.
  Color get lightBlue300 => const Color(0XFF53BDF8);

  /// Defines `Color(0XFF007AFF)`, a constant/variable used within the application.
  Color get lightBlueA700 => const Color(0XFF007AFF);

  // LightGreen
  /// Defines `Color(0XFFC9DD91)`, a constant/variable used within the application.
  Color get lightGreen200 => const Color(0XFFC9DD91);

  // Lime
  /// Defines `Color(0XFFE3E142)`, a constant/variable used within the application.
  Color get lime500 => const Color(0XFFE3E142);

  /// Defines `Color(0XFFADD243)`, a constant/variable used within the application.
  Color get lime600 => const Color(0XFFADD243);

  /// Defines `Color(0XFFBAB715)`, a constant/variable used within the application.
  Color get lime700 => const Color(0XFFBAB715);

  /// Defines `Color(0XFFB0AE0F)`, a constant/variable used within the application.
  Color get lime70001 => const Color(0XFFB0AE0F);

  /// Defines `Color(0XFFA8A606)`, a constant/variable used within the application.
  Color get lime800 => const Color(0XFFA8A606);

  /// Defines `Color(0XFF84820B)`, a constant/variable used within the application.
  Color get lime900 => const Color(0XFF84820B);

  /// Defines `Color(0XFF665E18)`, a constant/variable used within the application.
  Color get lime90001 => const Color(0XFF665E18);

  /// Defines `Color(0XFFEDEA48)`, a constant/variable used within the application.
  Color get limeA200 => const Color(0XFFEDEA48);

  // Orange
  /// Defines `Color(0XFFFF7C03)`, a constant/variable used within the application.
  Color get orange700 => const Color(0XFFFF7C03);

  /// Defines `Color(0XFFFFA34B)`, a constant/variable used within the application.
  Color get orangeA200 => const Color(0XFFFFA34B);

  /// Defines `Color(0XFFF59438)`, a constant/variable used within the application.
  Color get orangeA20001 => const Color(0XFFF59438);

  // Pink
  /// Defines `Color(0XFFEE536E)`, a constant/variable used within the application.
  Color get pink400 => const Color(0XFFEE536E);

  // Red
  /// Defines `Color(0XFFE77A6B)`, a constant/variable used within the application.
  Color get red300 => const Color(0XFFE77A6B);

  /// Defines `Color(0XFFFEEFE9)`, a constant/variable used within the application.
  Color get red50 => const Color(0XFFFEEFE9);

  /// Defines `Color(0XFFF05939)`, a constant/variable used within the application.
  Color get red500 => const Color(0XFFF05939);

  /// Defines `Color(0XFF913616)`, a constant/variable used within the application.
  Color get red900 => const Color(0XFF913616);

  /// Defines `Color(0XFF832B0B)`, a constant/variable used within the application.
  Color get red90001 => const Color(0XFF832B0B);

  // Teal
  /// Defines `Color(0XFFACDCD9)`, a constant/variable used within the application.
  Color get teal100 => const Color(0XFFACDCD9);

  /// Defines `Color(0XFFC8F6F4)`, a constant/variable used within the application.
  Color get teal50 => const Color(0XFFC8F6F4);

  /// Defines `Color(0XFFCDF0F2)`, a constant/variable used within the application.
  Color get teal5001 => const Color(0XFFCDF0F2);

  /// Defines `Color(0XFFD3E9EA)`, a constant/variable used within the application.
  Color get teal5002 => const Color(0XFFD3E9EA);

  /// Defines `Color(0XFF00904B)`, a constant/variable used within the application.
  Color get teal700 => const Color(0XFF00904B);

  // White
  /// Defines `Color(0XFFFDFDFC)`, a constant/variable used within the application.
  Color get whiteA700 => const Color(0XFFFDFDFC);

  /// Defines `Color(0XFFFFFFFF)`, a constant/variable used within the application.
  Color get whiteA70001 => const Color(0XFFFFFFFF);

  // Yellow
  /// Defines `Color(0XFFFFEB3B)`, a constant/variable used within the application.
  Color get yellow500 => const Color(0XFFFFEB3B);

  /// Defines `Color(0XFFF1ED15)`, a constant/variable used within the application.
  Color get yellowA400 => const Color(0XFFF1ED15);
}
